#include <stdio.h>
int main() {
    int a;
    printf("Enter an integer: ");  // Prompt the user for input
    scanf("%d", &a);
    printf("The output is %d\n", a);  // Added newline for better output formatting
    return 0;
}
